include(":app")
include(":calligraphy")
include(":gravitySnapHelper")
include(":overlappingPane")

rootProject.name = "summit"

pluginManagement {
    repositories {
        gradlePluginPortal()
        google()
        mavenCentral()
    }
}
