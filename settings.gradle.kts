include(":app")
include(":overlappingPane")
include(":calligraphy")

rootProject.name = "summit"

pluginManagement {
    repositories {
        gradlePluginPortal()
        google()
        mavenCentral()
    }
}
